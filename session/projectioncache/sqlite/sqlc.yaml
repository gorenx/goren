version: "2"
sql:
  - engine: "sqlite"
    schema: "sql/schema.sql"
    queries: "sql/query.sql"
    gen:
      go:
        package: "dbsql"
        out: "internal/dbsql"
        emit_empty_slices: true
